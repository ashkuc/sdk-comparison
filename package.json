{
  "name": "sdk-comparison",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "@polkadot/api": "^10.1.3",
    "@unique-nft/sdk": "^0.2.5",
    "@unique-nft/sr25519": "^0.0.0"
  }
}
